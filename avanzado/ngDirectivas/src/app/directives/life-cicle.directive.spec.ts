import { LifeCicleDirective } from './life-cicle.directive';

describe('LifeCicleDirective', () => {
  it('should create an instance', () => {
    const directive = new LifeCicleDirective();
    expect(directive).toBeTruthy();
  });
});
